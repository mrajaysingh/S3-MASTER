/**
 * S3 Master Admin - Main Script
 * Loads all module dependencies and initializes the application.
 */

// Load core modules
document.addEventListener('DOMContentLoaded', function() {
    // Core modules are loaded and initialized via admin-main.js
    console.log('S3 Master Admin modules loaded');
});

// For backwards compatibility, maintain jQuery wrapper
jQuery(document).ready(function($) {
    // All module initialization is now handled by admin-main.js
    console.log('S3 Master Admin Legacy compatibility loaded');
});
